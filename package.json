{
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "scripts": {
    "dev" : "concurrently \"docker start -a postgres\" \"cd backend && go run ./cmd\" \"cd frontend && npm start\"",
    "prod" : "concurrently \"docker start -a postgres\"  \"cd backend && go run ./cmd\" \"cd frontend && npm run build && serve -s build\""
  }
}
